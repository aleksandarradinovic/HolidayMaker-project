class hotelDescription {
    constructor(ID, gym, pool, restaurant, wifi)
    {
        this.ID = ID;
        this.gym = gym;
        this.pool = pool;
        this.restaurant = restaurant;
        this.wifi = wifi;
    }
}

module.exports ={
    hotelDescription
}